<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.example.myapplication"
    android:theme="@style/CustomTheme">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <supports-screens
        android:largeScreens="true"
        android:normalScreens="true"
        android:smallScreens="true"
        android:xlargeScreens="true" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.MyApplication">
        <activity android:name=".SplashActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".homepage"></activity>
        <activity android:name=".college_java"></activity>
        <activity android:name=".lawcolleges"
            android:label="@string/law"
            android:parentActivityName=".ui_back"/>
        <activity android:name=".mbacolleges"
            android:label="@string/mba"
            android:parentActivityName=".ui_back"/>
        <activity android:name=".engineeringcolleges"
            android:label="@string/engineer"
            android:parentActivityName=".ui_back"/>
        <activity android:name=".medicalcolleges"
            android:label="@string/medical"
            android:parentActivityName=".ui_back"/>
        <activity android:name=".ui_back">
        </activity>
        <activity
            android:name=".signup"
            tools:ignore="Instantiatable"></activity>
    </application>

</manifest>